# Profile picture

Drop your portrait here and name it **`portrait.jpg`** (or edit
`avatar.src` in `src/data/profile.ts` if you prefer a different file/name).

Current `portrait.jpg` was fetched automatically from the GitHub account this
repository uses (`GauravRathore11`). Replace it any time — the site picks it
up automatically and shows a "GR" monogram until the file exists.